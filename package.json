{
  "scripts": {
    "dev": "parcel index.html --no-cache",
    "build": "parcel build index.html --dist-dir=docs --no-cache --public-url=ton-demo",
    "pages": "yarn build && git add . && git commit -am 'pages' && git push"
  },
  "dependencies": {
    "@tonconnect/ui": "^2.0.3"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "process": "^0.11.10"
  }
}
